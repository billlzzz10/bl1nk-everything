server {
    listen 80;
    server_name localhost;

    # Set the root directory for static files
    root /usr/share/nginx/html;
    index index.html index.htm;

    # Specify charset to fix character encoding issues
    charset utf-8;

    location / {
        try_files $uri $uri/ =404;
    }
}
